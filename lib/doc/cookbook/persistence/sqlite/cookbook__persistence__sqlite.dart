import 'package:flutter/material.dart';

/// TODO https://flutter.dev/docs/cookbook/persistence/sqlite
class CookbookPersistenceSqlite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Persist Data With SQLite", style: TextStyle(letterSpacing: -0.5)),
      ),
      body: Text(""),
    );
  }
}
